# EMBERLIGHT dependencies.
#
# Linux/macOS: standard library only - nothing to install.
#
# Windows: Python does not ship curses. Install the only optional dependency:
#   pip install windows-curses
#
# This file intentionally lists no hard dependencies so a clean Linux
# checkout runs with zero installs.
