"""Raycasting / DDA renderer (DESIGN.md section 3).

Node 3 implements the per-column DDA raycaster, the per-column z-buffer, and
sprite projection here.  Node 2 ships only the module skeleton so the package
imports cleanly and the layout is fixed.
"""
