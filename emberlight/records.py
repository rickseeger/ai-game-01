"""Persistent local records (DESIGN.md section 12).

Load and save ``~/.emberlight/records.json`` (platform-appropriate path), and
the daily-seed records table.  Node 3 implements it; node 2 ships the
skeleton.
"""
